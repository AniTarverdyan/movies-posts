import { FC } from "react";
import { Styled } from "./style";

const Header : FC = () => {
return <Styled.Header>
    <Styled.Title>
    React Movie Posters

    </Styled.Title>
</Styled.Header>
};
export default Header;